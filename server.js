var http = require("http");var url = require("url");function start(route, handle) {  function onRequest(request, response) {    console.log("request.rawHeaders: " + request.rawHeaders);    /*    var index = request.rawHeaders.indexOf("Origin") + 1;    var origin = request.rawHeaders[index];    console.log("request.rawHeaders[" +index + "]: " + request.rawHeaders[index]);    */    // console.log("request.rawHeaders.length:" + request.rawHeaders.length);    var pathname = url.parse(request.url).pathname;    var query = url.parse(request.url).query;    console.log("\nRequest: pathname = " + pathname + ", query = ", query);    route(handle, pathname, query, response, request);  }  http.createServer(onRequest).listen(8888);  console.log("Server has started.")}exports.start = start;